# Arquivo de exemplo com as variáveis de ambiente usadas no projeto
# Copie este arquivo para `.env` e preencha com os valores reais.

# Porta do servidor (opcional)
PORT=5000

# --- Neon / Postgres (ex.: Neon) -------------------------------------------------
# Se você estiver usando Neon (Postgres), defina a URL de conexão abaixo.
# Exemplo (preencha com valores reais fornecidos pelo Neon):
# DATABASE_URL=postgresql://<user>:<password>@ep-name.region.neon.tech:5432/<database>?sslmode=require
# Algumas plataformas usam `DATABASE_URL` por convenção; mantemos ambas as variáveis
# para compatibilidade com diferentes setups.
DATABASE_URL=postgresql://usuario:senha@host:5432/nome_do_banco?sslmode=require
NEON_DATABASE_URL=
PGSSLMODE=require

# ---------------------------------------------------------------------------------

# Chave secreta para geração/validação de JWT
JWT_SECRET=sua_chave_jwt_aqui

# Exemplo de chave de API genérica (adicione outras chaves necessárias aqui)
# EX: CHAVE_API_SERVICO=sua_chave_api_aqui
CHAVE_API_EXEMPLO=

# Outras variáveis opcionais que você pode precisar:
# NODE_ENV=development
# EMAIL_HOST=smtp.exemplo.com
# EMAIL_USER=usuario
# EMAIL_PASS=senha

# Observação: NÃO coloque chaves reais neste arquivo de exemplo se for subir
# o repositório para um serviço público. Use apenas `.env` para valores reais.
